{%
  set title_classes = [
    label_display == 'visually_hidden' ? 'visually-hidden',
  ]
%}

{% if label_hidden %}
  {% if multiple %}
      {% for item in items %}
        <div class="text-secondaryColor banner_text">{{ item.content }}</div>
      {% endfor %}
  {% else %}
    {% for item in items %}
        <div class="text-secondaryColor banner_text">{{ item.content }}</div>
    {% endfor %}
  {% endif %}
{% else %}
    {% if multiple %}
    {% endif %}
    {% for item in items %}
      <div class="text-secondaryColor banner_text">{{ item.content }}</div>
    {% endfor %}
    {% if multiple %}
    {% endif %}
{% endif %}
</div>