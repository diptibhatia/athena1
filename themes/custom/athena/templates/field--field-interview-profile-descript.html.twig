{% if label_hidden %}
    {% for item in items %}
      <div{{ attributes }} class="col-sm-7 p-3">{{ item.content }}</div>
    {% endfor %}
  {% endif %}

</div>
</div>
</div>

                        <div class="col-md-2 col-sm-2">
      